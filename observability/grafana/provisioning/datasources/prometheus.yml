# Auto-provisions Prometheus as a Grafana datasource on container startup
# — no manual "Add data source" click-through. Grafana reads every .yml
# in this mounted directory at boot.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
